spring:
  # ==================== 数据源：对话记忆持久化到本地 MySQL ====================
  datasource:
    # createDatabaseIfNotExist=true：库不存在时自动创建，换机器也能直接跑起来
    url: jdbc:mysql://localhost:3306/purify_ai_agent?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 12345
    driver-class-name: com.mysql.cj.jdbc.Driver

  ai:
    # ---------------- 通义千问 DashScope ----------------
    dashscope:
      api-key: "sk-61cbfece8da24d2680c10b897fc7cbb6"
      # 原生 SDK 的根地址；带 /compatible-mode/v1 的是 OpenAI 兼容模式，此处不适用
      base-url: https://dashscope.aliyuncs.com

purify:
  rag:
    # 业务空间 ID。主账号默认空间留空即可，填了会作为 X-DashScope-WorkSpace 请求头发出去
    workspace-id: