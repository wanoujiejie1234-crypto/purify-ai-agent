/**
 * 两条链路的配置。
 * 主页卡片和聊天页都从这里取，主题色只有一份，不会出现「主页是青绿、聊天页是墨绿」这种偏差。
 *
 * **这里只剩和语言无关的东西**：链路名（同时也是接口路径段）、主题色、要不要展示工具过程。
 * 欢迎语 / 示例问题 / 输入框提示这些文案全部搬进了 `i18n/` —— 它们原先写在这里，
 * 而模块级常量是在 import 时求值一次的，运行期切语言不会重渲染，
 * 表现就是「界面全英文了，气泡里的欢迎语还是中文，刷新才好」。
 *
 * 标题（轻语 / PurifyManus）也进了语言包，虽然它是品牌名：英文界面里
 * 「轻语」得写成 `Qingyu`，否则对英文用户就是一个认不出的方块字。
 * 所以它跟着 locale 走，键是 `chat.{link}.title`。
 *
 * 取值一律走 `t(\`chat.${link}.xxx\`)`，键名前缀就是这里的 `link`，
 * 于是加一条链路只需要在语言包里加一组 `chat.<link>.*`，这个文件不用动。
 */

export const SLIM = {
  link: 'slim',
  accent: '#10a37f',
  /** 智能体链路才展示工具调用过程、才区分 WAITING_FOR_USER / ABORTED */
  agent: false,
}

export const MANUS = {
  link: 'manus',
  accent: '#6366f1',
  agent: true,
}
